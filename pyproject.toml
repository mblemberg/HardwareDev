[project]
name = "components"
version = "0.0.1"
description = "Component library: typed schemas, families, and instances for hw-analysis-framework."
requires-python = ">=3.11,<4.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
