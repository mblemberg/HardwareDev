"""Project-level configuration (design doc 6.4).

Submodules:
- :mod:`project.requirements` — typed external design constraints.
- ``project/scenarios.toml``   — environmental/operating corners (loaded via
                                 ``framework.load_scenarios``).
- ``project/modes.toml``       — system operating states (loaded via
                                 ``framework.load_modes``).
"""
